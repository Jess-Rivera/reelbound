#Run Locally 
npm run dev


#Run Production from GH and run
npm run build:gh && npm run preview
add /reelbound/ to the link

#Checkout from dev and push to dev
git checkout dev
git pull origin dev
# do your work
git add .
git commit -m "your changes"
git push origin dev
#When stable and ready to merge to main
git checkout main
git pull origin main
git merge dev
git push origin main



#First time clone
git clone https://github.com/Jess-Rivera/reelbound.git


#install dependencies
npm install

#Common Commands
Purpose	                        Command
See what changed	            git status
Stage all files	                git add .
Commit	                        git commit -m "message"
Push to GitHub	                git push origin main
Pull from GitHub	            git pull origin main
Check commit history	        git log --oneline